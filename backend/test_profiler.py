from agents.profiler import run_profiler_turn

result = run_profiler_turn(
    session_id="test-session-001",
    message="My budget is around 80k to 120k for the reception, wedding is next April, thinking pastel lehenga with minimal embroidery, based in Ahmedabad",
)

print(result)
